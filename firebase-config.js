// Paste your own Firebase project's web config here (from Firebase Console →
// Project settings → General → "Your apps" → the web app → Config).
// This is safe to leave in a public file / GitHub repo — the Firebase web
// API key is not a secret; access control happens via the security rules
// you set on Firestore + Authentication, not by hiding this key.
//
// Until you replace REPLACE_ME below, the app runs in local-only mode
// (saves to this device only, no cross-device sync).

export const firebaseConfig = {
  apiKey: "AIzaSyDl09qoPJd6_ojhojX0Zrzzt_vIT9eQgK8",
  authDomain: "cj-task-tracker.firebaseapp.com",
  projectId: "cj-task-tracker",
  storageBucket: "cj-task-tracker.firebasestorage.app",
  messagingSenderId: "1000921968169",
  appId: "1:1000921968169:web:b806fa782d3c831523f638",
};
